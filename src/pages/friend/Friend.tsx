import React, { memo } from 'react'

import TopBarBLine from '@/components/TopBarBLine';

export default memo(function Friend() {
  return (
    <div>
      <TopBarBLine />
      关注
    </div>
  )
})
